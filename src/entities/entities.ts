export interface Bouton  { id: number; type: string; quantite: number };
export interface Tissu  { id: number; couleur: string; quantite: number; categorie_id: number };
export interface Fil  { id: number; libelle: string };
export interface Aiguille  { id: number; libelle: string; quantite: number };
export interface Categorie  { id: number; libelle: string };
