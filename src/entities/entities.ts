export type Fil = { id: number; libelle: string };
export type Bouton = { id: number; type: string; quantite: number };
export type Tissu = { id: number; couleur: string; quantite: number; categorie_id: number };
export type Aiguille = { id: number; libelle: string; quantite: number };
export type Categorie = { id: number; libelle: string };
